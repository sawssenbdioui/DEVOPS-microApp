apiVersion: v1
kind: Service
metadata:
  name: expiration-service
spec:
  selector:
    app: expiration
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
